{
  "name": "ofoq-agent",
  "version": "4.0.0",
  "private": true,
  "description": "OFOQ Publisher Agent — Cloudflare Workers + Workflows",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev":    "wrangler dev",
    "tail":   "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
